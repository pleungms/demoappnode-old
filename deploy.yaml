apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: demonodeapp
spec:
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  minReadySeconds: 5 
  template:
    metadata:
      labels:
        app: demonodeapp
    spec:
      containers:
      - name: pleungtestapp
        image: pleungcon.azurecr.io/pleungms/demonodeapp:$BUILD_ID
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: demonodeapp
spec:
  type: LoadBalancer
  ports:
  - port: 80
  selector:
    app: demonodeapp